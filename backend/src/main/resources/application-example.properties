# Copy this file to application-local.properties, then fill in your local password.
spring.datasource.url=jdbc:mysql://localhost:3306/ai_learning_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=YOUR_MYSQL_ROOT_PASSWORD

# Generate a different Base64-encoded 32-byte value for every environment.
app.jwt.secret=YOUR_BASE64_JWT_SECRET
